from django.apps import AppConfig


class CategorymodelConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'categorymodel'
    verbose_name = "3. Kategoriler"
